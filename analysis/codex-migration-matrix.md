# Codex Migration Matrix

This matrix is the release gate for the Codex-only migration. `PASS` means the Codex entry point exists, its runtime dependencies no longer require Claude Code, and the listed regression coverage passed. Static knowledge used to audit Claude artifacts is intentionally retained.

## User commands

| Claude source | User behavior | Input | Output | State writes | Dependencies | Codex target | Regression test | Status |
|---|---|---|---|---|---|---|---|---|
| `commands/ls.md` (`/nlpm:ls`) | Discover and classify NL artifacts | empty, directory, or file path | Category A/B/F inventory and counts | none | scanner; shared discovery/classification | `$nlpm-ls`; `skills/nlpm/ls/SKILL.md`; scanner subagent | success, empty tree, invalid path | PASS |
| `commands/score.md` (`/nlpm:score`) | Score artifacts on the 100-point rubric; supports changed files | empty, path, or `--changed` | per-file findings, score table, summary | `.codex/nlpm-history.json` | scorer + vague scanner in parallel; Git; rubric skills | `$nlpm-score`; `skills/nlpm/score/SKILL.md` | normal, empty, invalid, `--changed`, parallel batches, snapshot | PASS |
| `commands/check.md` (`/nlpm:check`) | Check cross-artifact consistency | empty, directory, or file path | broken refs, orphans, contradictions, term drift | none | checker; discovery/classification | `$nlpm-check`; `skills/nlpm/check/SKILL.md`; `bin/nlpm-check` | normal, fewer than two artifacts, invalid path, overrides | PASS |
| `commands/fix.md` (`/nlpm:fix`) | Apply safe mechanical fixes and rescore | empty, directory, or file path | applied/skipped changes and before/after score | history through rescore | scorer; write tools; rubric | `$nlpm-fix`; `skills/nlpm/fix/SKILL.md` | fixable, no findings, invalid path, manual-only finding | PASS |
| `commands/trend.md` (`/nlpm:trend`) | Compare current scores with matching history | empty, directory, or file path | deltas, regressions, improvements | `.codex/nlpm-history.json` | scorer + vague scanner; history helper | `$nlpm-trend`; `skills/nlpm/trend/SKILL.md` | no history, matching history, mismatched scope, corrupt history | PASS |
| `commands/test.md` (`/nlpm:test`) | Evaluate NL-TDD specifications | empty or spec path | PASS/FAIL/RED results and totals | none | tester; `.nlpm-test/*.spec.md` | `$nlpm-test`; `skills/nlpm/test/SKILL.md` | all specs, one spec, no specs, malformed spec | PASS |
| `commands/init.md` (`/nlpm:init`) | Initialize project policy and baseline | optional project path | config plus baseline summary | `.codex/nlpm.local.md`, `.codex/nlpm-history.json`, `.gitignore` | discovery, scorer, history helper | `$nlpm-init`; `skills/nlpm/init/SKILL.md` | new project, existing config, legacy read-only import, invalid path | PASS |
| `commands/security-scan.md` (`/nlpm:security-scan`) | Scan executable plugin surfaces | empty, directory, or file path | severity findings and gate verdict | none | security scanner; hooks/scripts/MCP/dependency discovery | `$nlpm-security-scan`; `skills/nlpm/security-scan/SKILL.md` | safe, critical signature, no executable surface, invalid path | PASS |
| `commands/vocab-init.md` (`/nlpm:vocab-init`) | Build a canonical vocabulary registry | empty or project path | registry skill and opt-in configuration | vocabulary files; `.codex/nlpm.local.md` | vocabulary extractor; vocabulary conventions | `$nlpm-vocab-init`; `skills/nlpm/vocab-init/SKILL.md` | normal corpus, empty corpus, existing registry, invalid path | PASS |
| `commands/vocab-drift.md` (`/nlpm:vocab-drift`) | Advisory synonym/drift clustering | empty, directory, or file path | capped advisory clusters | none | vocab-drift scanner; discovery; batching | `$nlpm-vocab-drift`; `skills/nlpm/vocab-drift/SKILL.md` | normal, under-five corpus, batched corpus, invalid path | PASS |
| `commands/report.md` (`/nlpm:report`) | Render a self-contained HTML report | empty, directory, or file path | HTML report and data sidecar | `.codex/nlpm-reports/` | scorer, checker, drift scanner, `bin/nlpm-report` | `$nlpm-report`; `skills/nlpm/report/SKILL.md` | normal, empty, renderer failure, report links | PASS |
| `commands/spec-sync.md` (`/nlpm:spec-sync`) | Compare tool overlays with official specs and apply verified corrections | `claude`, `codex`, `antigravity`, or `all` | tagged gap reports, edits, verification summary | overlay and related reference edits | spec researcher; official docs; `bin/nlpm-check` | `$nlpm-spec-sync`; `skills/nlpm/spec-sync/SKILL.md` | each selector, invalid selector, low-confidence gap, validation failure | PASS |

## Runtime and delivery surfaces

| Claude source | User behavior | Input | Output | State writes | Dependencies | Codex target | Regression test | Status |
|---|---|---|---|---|---|---|---|---|
| `agents/*.md` | Role-specialized delegated analysis | artifact paths and bounded task | fixed role-specific result schemas | none | declared knowledge skills and read/write policy | `.codex/agents/*.toml` | parse all definitions; verify roles, policies, schemas, and command references | PASS |
| `commands/shared/*.md` | discovery, classification, snapshot persistence | paths/results | normalized inventory/history record | `.codex/nlpm-history.json` | Git and filesystem | `skills/nlpm/workflow-core/` plus deterministic helpers | discovery/classification/history tests | PASS |
| `hooks/hooks.json`; `scripts/check-artifact.sh` | advisory check after NL artifact edits | Codex hook JSON on stdin | advisory JSON/text; silent for non-targets | none | `PostToolUse`, `${PLUGIN_ROOT}` | `.codex/hooks.json`; portable hook script | target, non-target, malformed input/fail-open | PASS |
| `.mcp.json`; `.codex/config.toml` Claude bridge | optional local tool bridge | MCP configuration | tool registration | local Codex config only | Codex native MCP schema | `.codex/config.toml` without Claude bridge | TOML parse; forbidden server check | PASS |
| `.claude-plugin/`; `.codex-plugin/`; duplicate `codex/skills/` | install and discover plugin features | plugin install | skills, hooks, metadata | plugin cache outside repo | Codex plugin manifest and marketplace | `.codex-plugin/`; canonical `skills/nlpm/`; `.agents/skills` | manifest path existence and exhaustive registration | PASS |
| GitHub Actions using `claude-code-action` | automated audit/contribution/report generation | workflow events and repository data | patches, reports, PR preparation | preserved auditor data contracts | OpenAI API; Codex Action | `openai/codex-action@v1` | workflow scan; YAML/action input checks | PASS |
| `.claude/nlpm.local.md` and `.claude/nlpm-history.json` | project policy and trend persistence | configuration and snapshots | thresholds, overrides, trend baseline | Claude state directory | legacy read only | `.codex/nlpm.local.md`, `.codex/nlpm-history.json`; one-time read-only import | import and no-Claude-write tests | PASS |
| `README.md`, `docs/`, `site/`, `AGENTS.md`, `CLAUDE.md`, `GEMINI.md` | install/use/develop documentation | reader navigation | Codex-only runtime guidance | none | generated reference docs | Codex installation and `$nlpm-*` examples | forbidden runtime-reference gate | PASS |

## Release rule

The migration is complete only when every row is `PASS`, the full unit suite and `bin/nlpm-check .` are green, local Codex validation passes when available, and every remaining Claude-related search hit is classified as static audit knowledge, a fixture, or immutable historical evidence.
