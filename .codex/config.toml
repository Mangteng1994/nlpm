# Codex CLI project configuration for NLPM development.
# The published plugin is skills-only and needs no MCP server.

[features]
hooks = true
multi_agent = true

[agents]
enabled = true
max_concurrent_threads_per_session = 5
default_subagent_reasoning_effort = "high"
